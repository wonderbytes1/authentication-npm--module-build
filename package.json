{
  "name": "authentication-service",
  "version": "1.0.0",
  "description": "connect any api project to authentication microservice.",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^7.6.15",
    "@nestjs/core": "^7.5.1",
    "rxjs": "^6.6.7",
    "bull": "^3.20.0",
    "@nestjs/microservices": "^7.6.5"
  },
  "devDependencies": {
    "@types/node": "^14.14.37",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.3"
  }
}
